import "./NumLock.scss";
import "../Buttons/Buttons";

const NumLock = ({ current, operation, previous, setOperation }) => {
	return <div className="num-lock">{/* <Buttons /> */}</div>;
};

export default NumLock;
